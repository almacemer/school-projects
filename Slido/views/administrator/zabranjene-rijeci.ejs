<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="/javascripts/admin.js"></script>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/admin">Administrator</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/admin/predavaci">Predavači</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/predavanja">Predavanja</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/pitanja">Pitanja</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/admin/zabranjene-rijeci">Zabranjene riječi</a>
                </li>
            </ul>
            <form method="post" action="/logout" class="d-flex">
                <button type="submit" class="btn">Logout</button>
            </form>
        </div>
    </div>
</nav>

<div class="container">
    <div class="row">
        <div class="col-sm-5">
            <div class="d-flex">
                <input class="form-control me-2" type="text" name="nova-rijec" id="nova-rijec" placeholder="Unesite novu riječ">
                <button class="btn btn-outline-success" onclick="dodajRijec(<%= rijeci.length %>)">Dodaj</button>
            </div>
        </div>
    </div>
    <table id="tableID" style="cursor: pointer; text-align: center; margin-top: 2rem;" class="table table-responsive table-hover table-bordered align-middle bg-light">
        <thead>
        <tr>
            <th>#</th>
            <th>Zabranjene rijeci</th>
            <th>Akcija 1</th>
            <th>Akcija 2</th>
        </tr>
        </thead>
        <tbody id="tabela-body" style="text-align: center">
        <% for(let i = 0; i < rijeci.length; i++) { %>
            <tr id="rijec<%= rijeci[i].id %>">
                <td><%=i+1%></td>
                <td><input type="text" class="form-control" value="<%= rijeci[i].rijec %>" id="<%= rijeci[i].id %>"></td>
                <td><button class="btn btn-primary" onclick="urediRijec(<%= rijeci[i].id %>)">Uredi</button></td>
                <td><button class="btn btn-danger" onclick="obrisiRijec(<%= rijeci[i].id %>)">Obriši</button></td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
    <div class="container fixed-bottom">
        <footer class="my-2">
            <ul class="nav justify-content-center border-bottom pb-3 mb-3">
            </ul>
            <p class="text-center text-muted">© 2023 Projekat Slido</p>
        </footer>
    </div>
</body>